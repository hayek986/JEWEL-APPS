name: newhayekjewel_admin # تأكد من أن هذا هو اسم مشروع الأدمن الخاص بك
description: "A new Flutter project for Admin."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.25.3
  firebase_messaging: ^14.9.0
  cloud_firestore: ^4.15.3
  firebase_storage: ^11.6.6
  image_picker: ^1.0.4
  provider: ^6.1.1
  flutter_local_notifications: ^17.0.0 # <--- تم التحديث إلى 17.0.0
  intl: ^0.18.1
  firebase_auth: ^4.17.1
  cached_network_image: ^3.3.0
  marquee: ^2.2.0
  shared_preferences: ^2.2.2
  flutter_dotenv: ^5.1.0
  http: ^1.1.2
  html: ^0.15.4
  carousel_slider: ^4.2.1
  url_launcher: ^6.1.10
  uuid: ^4.4.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/bk.png # تأكد من وجود صورة الخلفية في هذا المسار
    - .env # تأكد من إضافة هذا المسار إذا كنت تستخدم ملف .env
